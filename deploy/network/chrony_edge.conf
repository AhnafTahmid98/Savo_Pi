# Prefer core over Internet pools; pools provide failover while Wi-Fi exists.
server @CORE_ETH_IP@ iburst prefer
pool pool.ntp.org iburst maxsources 2
makestep 1.0 3
rtcsync
driftfile /var/lib/chrony/chrony.drift
logdir /var/log/chrony
