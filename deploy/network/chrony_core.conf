# Robot SAVO core is the preferred time source on the isolated Ethernet link.
pool pool.ntp.org iburst maxsources 4
makestep 1.0 3
rtcsync
allow @LINK_SUBNET@
local stratum 10 orphan
driftfile /var/lib/chrony/chrony.drift
logdir /var/log/chrony
