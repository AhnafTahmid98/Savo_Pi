realsense2_camera:
  ros__parameters:
    camera_name: camera
    camera_namespace: camera

    serial_no: ""
    usb_port_id: ""
    device_type: ""

    enable_color: true
    enable_depth: true
    enable_infra1: false
    enable_infra2: false
    enable_gyro: false
    enable_accel: false

    color_width: 640
    color_height: 480
    color_fps: 30

    depth_width: 640
    depth_height: 480
    depth_fps: 30

    depth_module.depth_profile: "640x480x30"
    rgb_camera.color_profile: "640x480x30"

    align_depth.enable: true
    pointcloud.enable: true
    pointcloud.stream_filter: 2
    pointcloud.stream_index_filter: 0
    pointcloud.ordered_pc: false

    enable_sync: true
    # Fixed camera TF is owned by savo_description/robot_state_publisher.
    publish_tf: false
    tf_publish_rate: 0.0

    initial_reset: false
    reconnect_timeout: 6.0
    wait_for_device_timeout: -1.0

camera_topic_monitor_node:
  ros__parameters:
    publish_hz: 2.0
    stale_timeout_s: 0.50

    expected_color_hz: 30.0
    expected_depth_hz: 30.0
    expected_aligned_depth_hz: 30.0
    expected_camera_info_hz: 30.0
    expected_pointcloud_hz: 10.0

    require_pointcloud: true
    require_aligned_depth: false

camera_health_node:
  ros__parameters:
    status_hz: 2.0
    stale_timeout_s: 0.50
    depth_signal_topic: /depth/min_front_m
    vo_health_topic: /vo/health
    obstacle_cloud_health_topic: /savo_perception/obstacle_cloud/health
    require_depth_signal: true
    require_vo_health: false
    require_obstacle_cloud_health: false

depth_front_min_node:
  ros__parameters:
    input_topic: /camera/camera/depth/image_rect_raw
    output_topic: /depth/min_front_m

    depth_scale: 0.001
    min_valid_m: 0.02
    max_valid_m: 3.00
    percentile: 10.0

    x_min_ratio: 0.35
    x_max_ratio: 0.65
    y_min_ratio: 0.35
    y_max_ratio: 0.75
