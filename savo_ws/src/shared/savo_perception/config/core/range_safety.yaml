# Robot Savo range safety fusion configuration.
# C++ node is default. Python node uses the same parameter contract as fallback.

safety_stop_node:
  ros__parameters:
    depth_front_topic: /depth/min_front_m
    tof_left_topic: /savo_perception/range/left_m
    tof_right_topic: /savo_perception/range/right_m
    ultrasonic_front_topic: /savo_perception/range/front_ultrasonic_m

    safety_stop_topic: /safety/stop
    slowdown_topic: /safety/slowdown_factor
    safety_state_topic: /savo_perception/safety_state

    loop_hz: 20.0
    stale_timeout_s: 0.30

    front_stop_m: 0.28
    front_slow_m: 0.60
    side_stop_m: 0.20
    side_slow_m: 0.45

    front_clear_hysteresis_m: 0.06
    side_clear_hysteresis_m: 0.05

    stop_debounce_count: 2
    clear_debounce_count: 4

    slowdown_min: 0.20
    slowdown_max: 1.0
    slowdown_ema_alpha: 0.35

    fail_safe_on_stale: true

    required_sensors:
      - tof_left
      - tof_right
      - ultrasonic_front

    optional_sensors:
      - depth_front

    use_depth_front: true
    depth_front_required: false

    publish_state_json: true
    startup_fail_is_fatal: false

safety_stop_node_py:
  ros__parameters:
    depth_front_topic: /depth/min_front_m
    tof_left_topic: /savo_perception/range/left_m
    tof_right_topic: /savo_perception/range/right_m
    ultrasonic_front_topic: /savo_perception/range/front_ultrasonic_m

    safety_stop_topic: /safety/stop
    slowdown_topic: /safety/slowdown_factor
    safety_state_topic: /savo_perception/safety_state

    loop_hz: 20.0
    stale_timeout_s: 0.30

    front_stop_m: 0.28
    front_slow_m: 0.60
    side_stop_m: 0.20
    side_slow_m: 0.45

    front_clear_hysteresis_m: 0.06
    side_clear_hysteresis_m: 0.05

    stop_debounce_count: 2
    clear_debounce_count: 4

    slowdown_min: 0.20
    slowdown_max: 1.0
    slowdown_ema_alpha: 0.35

    fail_safe_on_stale: true

    required_sensors:
      - tof_left
      - tof_right
      - ultrasonic_front

    optional_sensors:
      - depth_front

    use_depth_front: true
    depth_front_required: false

    publish_state_json: true
    startup_fail_is_fatal: false