"/**":
  ros__parameters:
    core_ups_expected: false
    edge_ups_expected: true
    base_battery_expected: false

    publish_rate_hz: 1.0
    stale_timeout_s: 5.0

    # Core owns shutdown decisions.
    automatic_shutdown_enabled: false
